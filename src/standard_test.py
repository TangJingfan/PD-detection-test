import pygame
import random
import time

# init pygame structure
pygame.init()

# size of the window
screen_height, screen_width = 600, 800
screen = pygame.display.set_mode((screen_width, screen_height))

# define color set
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)

# quit pygame structure
pygame.quit()